[package]
name = "zap_sftp"
version = "0.1.0"
edition = "2024"

[dependencies]
ssh2 = { version = "0.9", features = ["openssl-on-win32"] }
openssl-sys = { version = "*", features = ["vendored"] }
smol = "2"
thiserror = "2"
dirs.workspace = true
log.workspace = true

[dev-dependencies]
tempfile = "3"
