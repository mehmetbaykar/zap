# cargo-i18n configuration (used by the `cargo i18n` CLI to validate/extract/sync translation keys)
# Docs: https://github.com/kellpossible/cargo-i18n
fallback_language = "en"

[fluent]
# Translation assets root directory (one subdirectory per locale, with .ftl files inside each locale directory)
assets_dir = "i18n"
